import { EliminaActivosPipe } from './elimina-activos.pipe';

describe('EliminaActivosPipe', () => {
  it('create an instance', () => {
    const pipe = new EliminaActivosPipe();
    expect(pipe).toBeTruthy();
  });
});
